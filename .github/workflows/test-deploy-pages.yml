name: Check if a deploy of the documentation would run successfully

on:
  push:
    branches:
      - "!main"
  pull_request:
    branches:
      - main

jobs:
  build:
    name: Build Docusaurus
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm
          cache-dependency-path: documentation

      - name: Install dependencies
        working-directory: documentation
        run: npm ci
      - name: Build website
        working-directory: documentation
        run: npm run build
